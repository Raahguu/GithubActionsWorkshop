name: compile-and-test-C-program

on: workflow_dispatch

jobs:
  build:
    strategy:
      matrix:
        compiler: [gcc, clang]
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      
      - name: Install ${{ matrix.compiler }}
        run: "sudo apt-get install -y ${{ matrix.compiler }}"
        
      - name: Compile code
        run: "${{ matrix.compiler }} hello.c -o ${{ matrix.compiler }}_hello.exe"

      - name: Run tests
        run: |
          if ./${{ matrix.compiler }}_hello.exe; then
            echo "Test Passed"
          else
            echo "Test Failed"
            exit 1
          fi
      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.compiler }}_hello.exe
          path: ${{ matrix.compiler }}_hello.exe
